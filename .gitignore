node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# Local Catalyst project/environment binding — per-machine, not source
.catalystrc
# Generated by the CLI; holds an absolute source path, so it is machine-specific
catalyst.json
catalyst-debug.log

# Chunks from previous Slate releases, merged forward at deploy time
.slate-static-archive

# Local-only secrets injected at deploy time — never committed
.secrets.env

# Dated registry snapshots — regenerate with scripts/backup-windfarms.sh
backups/
